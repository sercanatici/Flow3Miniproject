const Position = require("../models/Position")


/*

async function addPosition(author, pos) {

    var position = new Position 
    ({ 
        author,
        pos
    });
    
    await Position.save(position);
    return position;
    //await location.create();
}
*/